---
tags:
  - 数学
  - 公式
dlink:
  - "[[--积分变换--]]"
aliases:
  - δ 函数
  - 狄拉克函数
  - unit-impulse function
author:
  - Reflect314
finished: false
---

傅里叶级数与傅里叶变换以不同的形式反映了周期函数与非周期函数的频谱特性, 是否可以借助某种手段将它们统一起来呢? 更具体地说, 是否能够将离散频谱以连续频谱的方式表现出来呢? 这就需要引入下面将要介绍的单位冲激函数, 其中单位冲激函数本身有其实际背景. 在工程实际问题中, 有许多物理现象具有一种脉冲特征, 它们仅在某一瞬间或者某一点出现, 如瞬时冲击力、脉冲电流、质点的质量等, 这些物理量都不能用通常的函数形式去描述.

## δ 函数的定义及其性质

定义单位冲激函数 $\delta (t)$ 是满足下面两个条件的函数:

1. 当 $t \neq 0$ 时, $\delta (t) = 0$ ;
2. $\int_{-\infty}^{+\infty}\delta (t)\mathrm{d}t = 1.$

上述定义方式在理论上是不严格的, 它只是对 $\delta$ 函数的某种描述. 事实上, $\delta$ 函数并不是经典意义上的函数, 而是一个广义函数, 因此, 关于 $\delta$ 函数的严格定义可参阅有关广义函数方面的书籍. 另外, $\delta$ 函数在现实生活中也是不存在的, 它是数学抽象的结果. 有时人们将 $\delta$ 函数直观的理解为 $\delta (t)=\lim_{\varepsilon\to0}\delta_{\varepsilon}(t)$ ,其中 $\delta_{\varepsilon}(t)$ 是宽度为 $\varepsilon$ ,高度为 $1/\varepsilon$ 的矩形冲激函数.

```tikz
\begin{document}
\begin{tikzpicture}[scale=1, thick, >=stealth]
  % Draw axes
  \draw[->] (-0.3,0) -- (2.5,0) node[right] {$t$};
  \draw[->] (0,-0.2) -- (0,2.8) node[above] {$\delta_\varepsilon(t)$};
  
  % Draw the rectangular pulse (filled)
  \fill[gray!40] (0,0) rectangle (1,2.2);
  \draw (0,0) -- (0,2.2) -- (1,2.2) -- (1,0);
  
  % Draw tick marks
  \draw (1,0.05) -- (1,-0.05);
  
  % Labels
  \node[below] at (1,0) {$\varepsilon$};
  \node[right] at (1.05,2.1) {$\frac{1}{\varepsilon}$};
\end{tikzpicture}
\end{document}
```

下面我们不加证明地直接给出 $\delta$ 函数的几个基本性质.

- 筛选性质：
    - 设 $f (t)$ 是定义在实数域 $\mathbf{R}$ 上的有界函数，且在 $t = 0$ 处连续，则 $\int_{- \infty}^{+ \infty} \delta (t) f (t) \mathrm{d} t = f (0)$
    - 一般地，若 $f (t)$ 在 $t=t_{0}$ 点连续，则 $\int_{- \infty}^{+ \infty} \delta (t - t_{0}) f (t) \mathrm{d} t = f (t_{0})$
    - 该性质给出了 $\delta$ 函数与其他函数的运算关系, 它也常常被人们用来定义 $\delta$ 函数, 即采用检验的方式来考察某个函数是否为 $\delta$ 函数.
- 对偶性质：δ 函数为偶函数, 即 $\delta (t)=\delta (-t)$ .
- 导数性质：设 $u (t)$ 为单位阶跃函数, 即 $u (t) = \left\{ \begin{array}{l l} 1, & t > 0, \\ 0, & t <   0, \end{array} \right.$，则有 $\int_{-\infty}^{t}\delta (\tau)\mathrm{d}\tau = u (t),\frac{\mathrm{d}[u (t)]}{\mathrm{d}t} = \delta (t).$

在图形上, 人们常常采用一个从原点出发长度为 1 的有向线段来表示 $\delta$ 函数 (图a), 其中有向线段的长度代表 $\delta$ 函数的积分值, 称为冲激强度. 图(b) 与图(c) 则分别为函数 $A\delta (t)$ 与 $\delta (t-t_{0})$ 的图形表示, 其中 $A$ 为 $A\delta (t)$ 的冲激强度.

```tikz
\begin{document}
\begin{tikzpicture}[scale=1, thick, >=stealth]
  % Draw axes
  \draw[->] (-1.5,0) -- (2.5,0) node[right] {$t$};
  \draw[->] (0,-0.8) -- (0,3.5) node[right] {$\delta(t)$};
  
  % Draw the impulse arrow at origin
  \draw[line width=2.5pt][->] (0,0) -- (0,2.5);
  
  % Label (1) next to the impulse
  \node[right] at (0.15,2.2) {$(1)$};
\end{tikzpicture}
\end{document}
```

(a)

```tikz
\begin{document}
\begin{tikzpicture}[scale=1, thick, >=stealth]
  % Draw axes
  \draw[->] (-1.5,0) -- (2.5,0) node[right] {$t$};
  \draw[->] (0,-0.8) -- (0,3.5) node[right] {$A\delta(t)$};
  
  % Draw the impulse arrow at origin
  \draw[line width=2.5pt][->] (0,0) -- (0,2.5);
  
  % Label (1) next to the impulse
  \node[right] at (0.15,2.2) {$(A)$};
\end{tikzpicture}
\end{document}
```

(b)

```tikz
\begin{document}
\begin{tikzpicture}[scale=1, thick, >=stealth]
  % Draw axes
  \draw[->] (-1.5,0) -- (3.5,0) node[right] {$t$};
  \draw[->] (0,-0.8) -- (0,3.5) node[right] {$\delta(t-t_0)$};
  
  % Tick mark on y-axis
  \draw (-0.08,2) -- (0.08,2);
  
  % Tick mark on x-axis (left side)
  \draw (-0.8,-0.08) -- (-0.8,0.08);
  
  % Draw the impulse arrow at t_0
  \draw[line width=2.5pt][->] (2,0) -- (2,2.2);
  
  % Labels
  \node[below] at (2,0) {$t_0$};
  \node[right] at (2.15,2.1) {$(1)$};
\end{tikzpicture}
\end{document}
```

(c)

> [!NOTE]- 例题：给出函数 $F (\omega)=\pi[\delta (\omega-\omega_{0})-\delta (\omega+\omega_{0})]$ 的图形表示, 其中 $\omega_{0}>0$ .
>
> 解：
>
> 函数 $F (\omega) = \pi [\delta (\omega -\omega_0) - \delta (\omega +\omega_0)]$ 在 $\omega_0$ 与 $-\omega_0$ 的冲激强度分别为 $\pi$ 和 $-\pi$ ，其图形如图所示.
>
> ```tikz
> \begin{document}
> \begin{tikzpicture}[scale=1, thick, >=stealth]
>   % Draw axes
>   \draw[->] (-3.5,0) -- (3.5,0) node[right] {$\omega$};
>   \draw[->] (0,-2.5) -- (0,3.5) node[right] {$F(\omega)$};
>   
>   % Tick marks on x-axis
>   \draw (-2,-0.08) -- (-2,0.08);
>   \draw (2,-0.08) -- (2,0.08);
>   
>   % Labels on x-axis
>   \node[above] at (-2,0.1) {$-\omega_0$};
>   \node[below] at (2,-0.1) {$\omega_0$};
>   
>   % Upward impulse at omega_0
>   \draw[line width=2.5pt][->] (2,0) -- (2,2.2);
>   \node[right] at (2.15,2.1) {$(\pi)$};
>   
>   % Downward impulse at -omega_0
>   \draw[line width=2.5pt][->] (-2,0) -- (-2,-2.2);
>   \node[left] at (-2.15,-2.1) {$(-\pi)$};
> \end{tikzpicture}
> \end{document}
> ```
>
> 本例显示, 利用单位冲激函数可以将离散值以连续形式表达, 从而可以统一地进行处理.

## δ 函数的傅里叶变换

根据 $\delta$ 函数的定义, 我们可以容易地得出 $\delta$ 函数的傅氏变换为

$$
\mathcal {F} [ \delta (t) ] = \int_{- \infty}^{+ \infty} \delta (t) \mathrm{e}^{- \mathrm{j} \omega t} \mathrm{d} t = \mathrm{e}^{- \mathrm{j} \omega t} \Bigg |_{t = 0} = 1.
$$

即单位冲激函数包含各种频率分量且它们具有相等的幅度, 称此为均匀频谱或白色频谱. 由此得出, $\delta (t)$ 与 1 构成傅氏变换对, 按逆变换公式有

$$
\mathcal {F}^{- 1} [ 1 ] = \frac {1}{2 \pi} \int_{- \infty}^{+ \infty} \mathrm{e}^{\mathrm{j} \omega t} \mathrm{d} \omega = \delta (t)
$$

这是一个关于 $\delta$ 函数的重要公式.

需要注意的是, 这里 $\delta (t)$ 的傅里叶变换仍采用傅里叶变换的古典定义, 但此时的反常积分是根据 $\delta$ 函数的定义和运算性质直接给出的, 而不是普通意义下的积分值, 故称 $\delta (t)$ 的傅里叶变换是一种广义的傅里叶变换. 运用这一概念, 可以对一些常用的函数, 如常数函数、单位阶跃函数以及正、余弦函数进行傅氏变换, 尽管它们并不满足绝对可积条件. 下面通过几个例子给予说明.

> [!NOTE]- 例题：分别求函数 $f_{1}(t)=1$ 与 $f_{2}(t)=\mathrm{e}^{\mathrm{j}\omega_{0}t}$ 的傅氏变换.
>
> 解：
>
> 由傅氏变换的定义以及逆变换公式 $\mathcal {F}^{- 1} [ 1 ] = \frac {1}{2 \pi} \int_{- \infty}^{+ \infty} \mathrm{e}^{\mathrm{j} \omega t} \mathrm{d} \omega = \delta (t)$ 有
>
> $$\begin{align}&F_{1} (\omega) = \mathcal {F} [ f_{1} (t) ] = \int_{- \infty}^{+ \infty} \mathrm{e}^{- \mathrm{j} \omega t} \mathrm{d} t = \int_{- \infty}^{+ \infty} \mathrm{e}^{\mathrm{j} \omega \tau} \mathrm{d} \tau = 2 \pi \delta (\omega), \\ &F_{2} (\omega) = \mathcal {F} [ f_{2} (t) ] = \int_{- \infty}^{+ \infty} \mathrm{e}^{\mathrm{j} \omega_{0} t} \mathrm{e}^{- \mathrm{j} \omega t} \mathrm{d} t = \int_{- \infty}^{+ \infty} \mathrm{e}^{\mathrm{j} (\omega_{0} - \omega) t} \mathrm{d} t = 2 \pi \delta (\omega_{0} - \omega) = 2 \pi \delta (\omega - \omega_{0}). \end{align}$$

> [!NOTE]- 例题：证明: 单位阶跃函数 $u (t)$ 的傅氏变换为 $\frac{1}{j\omega} + \pi\delta (\omega)$
>
> 证设 $F (\omega) = \frac{1}{\mathrm{j}\omega} + \pi \delta (\omega), f (t) = \mathcal{F}^{-1}[F (\omega)]$ ，则
>
> $$\begin{array}{r l} f (t) & = \frac {1}{2 \pi} \int_{- \infty}^{+ \infty} \left[ \frac {1}{\mathrm{j} \omega} + \pi \delta (\omega) \right] \mathrm{e}^{\mathrm{j} \omega t} \mathrm{d} \omega \\ & = \frac {1}{2} \int_{- \infty}^{+ \infty} \delta (\omega) \mathrm{e}^{\mathrm{j} \omega t} \mathrm{d} \omega + \frac {1}{2 \pi} \int_{- \infty}^{+ \infty} \frac {1}{\mathrm{j} \omega} \mathrm{e}^{\mathrm{j} \omega t} \mathrm{d} \omega \\ & = \frac {1}{2} + \frac {1}{\pi} \int_{0}^{+ \infty} \frac {\sin \omega t}{\omega} \mathrm{d} \omega , \end{array}$$
>
> 因为 $\int_0^{+\infty}\frac{\sin x}{x}\mathrm{d}x = \frac{\pi}{2}$ ，所以有$$f (t) = u (t) = \left\{ \begin{array}{l l} 1, & t > 0, \\ 0, & t <   0. \end{array} \right.$$

> [!NOTE]- 例题：求 $f (t)=\cos\omega_{0}t$ 的傅氏变换.
>
> 解：
>
> 由傅氏变换的定义有$$\begin{align}F (\omega) = \mathcal {F} [ f (t) ] &= \int_{- \infty}^{+ \infty} \mathrm{e}^{- \mathrm{j} \omega t} \cos \omega_{0} t \mathrm{d} t \\ &= \int_{- \infty}^{+ \infty} \frac {1}{2} \left (\mathrm{e}^{\mathrm{j} \omega_{0} t} + \mathrm{e}^{- \mathrm{j} \omega_{0} t}\right) \mathrm{e}^{- \mathrm{j} \omega t} \mathrm{d} t \\ &= \frac {1}{2} \int_{- \infty}^{+ \infty} \left[ \mathrm{e}^{- \mathrm{j} (\omega - \omega_{0}) t} + \mathrm{e}^{- \mathrm{j} (\omega + \omega_{0}) t} \right] \mathrm{d} t \\ &= \pi [ \delta (\omega - \omega_{0}) + \delta (\omega + \omega_{0}) ].\end{align}$$
>
> 本例显示,在广义傅氏变换意义下,周期函数也可以进行傅氏变换,其频谱仍是离散的 (见下图), 这一点与傅氏级数展开是一致的. 所不同的是, 这里是用冲激强度来表示各频率分量的幅值的相对大小.
>
> ```tikz
> \begin{document}
> \begin{tikzpicture}[scale=1, thick, >=stealth]
>   % Draw axes
>   \draw[->] (-3.5,0) -- (3.5,0) node[right] {$\omega$};
>   \draw[->] (0,-2.5) -- (0,3.5) node[right] {$F(\omega)$};
>   
>   % Tick marks on x-axis
>   \draw (-2,-0.08) -- (-2,0.08);
>   \draw (2,-0.08) -- (2,0.08);
>   
>   % Labels on x-axis
>   \node[below] at (-2,-0.1) {$-\omega_0$};
>   \node[below] at (2,-0.1) {$\omega_0$};
>   
>   % Upward impulse at omega_0
>   \draw[line width=2.5pt][->] (2,0) -- (2,2.2);
>   \node[right] at (2.15,2.1) {$(\pi)$};
>   
>   % Upward impulse at -omega_0
>   \draw[line width=2.5pt][->] (-2,0) -- (-2,2.2);
>   \node[left] at (-2.15,2.1) {$(-\pi)$};
> \end{tikzpicture}
> \end{document}
> ```
>

^e9e81c

事实上, 对周期函数而言, 有如下定理：

设 $f (t)$ 是以 $T$ 为周期的实值函数，且在 $\left[-\frac{T}{2}, \frac{T}{2}\right]$ 上满足狄氏条件，则 $f (t)$ 和 $F (\omega) = \sum_{n=-\infty}^{+\infty} 2\pi F (n\omega_0) \delta (\omega - n\omega_0)$ 是一对傅氏变换，其中， $\omega_0 = \frac{2\pi}{T}$ ， $F (n\omega_0)$ 是 $f (t)$ 的离散频谱。

证：按傅氏级数展开式有

$$
f (t) = \sum_{n = - \infty}^{+ \infty} F (n \omega_{0}) \mathrm{e}^{\mathrm{j} n \omega_{0} t},
$$

因此

$$
\begin{align}
&\begin{array}{r l} \mathcal {F} [ f (t) ] & = \int_{- \infty}^{+ \infty} f (t) \mathrm{e}^{- \mathrm{j} \omega t} \mathrm{d} t = \sum_{n = - \infty}^{+ \infty} F (n \omega_{0}) \int_{- \infty}^{+ \infty} \mathrm{e}^{\mathrm{j} n \omega_{0} t} \mathrm{e}^{- \mathrm{j} \omega t} \mathrm{d} t \ & = \sum_{n = - \infty}^{+ \infty} 2 \pi F (n \omega_{0}) \delta (\omega - n \omega_{0}) = F (\omega), \end{array} \\
&\begin{array}{r l} \mathcal {F}^{- 1} [ F (\omega) ] & = \frac {1}{2 \pi} \int_{- \infty}^{+ \infty} F (\omega) \mathrm{e}^{\mathrm{j} \omega t} \mathrm{d} \omega = \sum_{n = - \infty}^{+ \infty} F (n \omega_{0}) \int_{- \infty}^{+ \infty} \delta (\omega - n \omega_{0}) \mathrm{e}^{\mathrm{j} \omega t} \mathrm{d} \omega \\ & = \sum_{n = - \infty}^{+ \infty} F (n \omega_{0}) \mathrm{e}^{\mathrm{j} n \omega_{0} t} = f (t), \end{array}
\end{align}
$$

即得 $f (t)$ 与 $F (\omega)$ 是一对傅氏变换.
